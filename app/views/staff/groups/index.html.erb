<section role="main" class="content-body">
  <header class="page-header">
    <h2>Senarai Kumpulan</h2>
    <div class="right-wrapper pull-right">
      <ol class="breadcrumbs">
        <li>
          <%= link_to root_path do %>
            <i class="fa fa-home"></i>
          <% end %>
        </li>
        <li>
          <span>Senarai Kumpulan <%= @zone.title %></span>
        </li>
      </ol>
      <a class="sidebar-right-toggle" data-open="sidebar-right">
        <i class="fa fa-chevron-left"></i>
      </a>
    </div>
  </header>
  <div class="row">
    <div class="col-md-12">
      <section class="panel">
        <header class="panel-heading">
          <h2 class="panel-title">Senarai Kumpulan bagi <%= @zone.title %></h2>
        </header>
        <div class="panel-body">
          <!-- Main Content Start -->
          <table class="table table-bordered table-striped mb-none" id="datatable-lbr">
            <thead>
              <tr>
                <th class="col-md-1">Bil</th>
                <th>Nama Zon</th>
              </tr>
            </thead>
            <tbody>
              <% @staff_groups.each_with_index do |group,index| %>
              <tr>
                <td><%= index + 1 %></td>
                <td><%= group.name %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
          <!-- Main Content End -->
        </div>
        <div class="panel-footer">
          <%= link_to "Daftar Kumpulan", new_staff_zone_group_path, class: "btn btn-primary" %>
          <%= link_to "Kembali Ke Zon", staff_zones_path, class: "btn btn-default" %>
        </div>
      </section>
    </div>
  </div>
</section>
