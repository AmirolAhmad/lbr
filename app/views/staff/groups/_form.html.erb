<% if @staff_group.errors.any? %>
<div class="alert alert-danger alert-dismissable">
  <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
  <strong><%= pluralize(@staff_group.errors.count, "error") %> prohibited
    this user from being saved:</strong>
    <ul>
      <% @staff_group.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
</div>
<% end %>

<div class="form-group">
  <%= f.label :name, "Nama Kumpulan", class: "col-md-3 control-label" %>
  <div class="col-md-6">
    <%= f.text_field :name, :class => "form-control" %>
  </div>
</div>

<div class="form-group">
  <label class="col-md-2 control-label"></label>
  <div class="col-md-10">
    <%= f.button "Simpan", :class => "btn btn-primary", data: { disable_with: "<i class='fa fa-spinner fa-spin'></i> Saving..." } %>
    <%= link_to 'Kembali', staff_zone_groups_path, :class => "btn btn-link" %>
  </div>
</div>
